name: Release Artifacts

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux x64
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            cli_artifact: vet-linux-x64
            lsp_artifact: vet-lsp-linux-x64

          # Linux ARM64
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            cli_artifact: vet-linux-arm64
            lsp_artifact: vet-lsp-linux-arm64
            cross: true

          # macOS x64 (Intel)
          - os: macos-latest
            target: x86_64-apple-darwin
            cli_artifact: vet-darwin-x64
            lsp_artifact: vet-lsp-darwin-x64

          # macOS ARM64 (Apple Silicon)
          - os: macos-latest
            target: aarch64-apple-darwin
            cli_artifact: vet-darwin-arm64
            lsp_artifact: vet-lsp-darwin-arm64

          # Windows x64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            cli_artifact: vet-windows-x64.exe
            lsp_artifact: vet-lsp-windows-x64.exe

          # Windows ARM64
          - os: windows-latest
            target: aarch64-pc-windows-msvc
            cli_artifact: vet-windows-arm64.exe
            lsp_artifact: vet-lsp-windows-arm64.exe

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: ${{ matrix.target }}

      - name: Install cross-compilation tools (Linux ARM64)
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc" >> "$GITHUB_ENV"

      - name: Build CLI
        run: cargo build --release --package vet_cli --target ${{ matrix.target }}

      - name: Build LSP
        run: cargo build --release --package vet_lsp --target ${{ matrix.target }}

      - name: Prepare artifacts (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p staging

          cp "target/${{ matrix.target }}/release/vet" "staging/${{ matrix.cli_artifact }}"
          chmod +x "staging/${{ matrix.cli_artifact }}"

          cp "target/${{ matrix.target }}/release/vet-lsp" "staging/${{ matrix.lsp_artifact }}"
          chmod +x "staging/${{ matrix.lsp_artifact }}"

      - name: Prepare artifacts (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path staging

          Copy-Item "target/${{ matrix.target }}/release/vet.exe" "staging/${{ matrix.cli_artifact }}"
          Copy-Item "target/${{ matrix.target }}/release/vet-lsp.exe" "staging/${{ matrix.lsp_artifact }}"

      - name: Upload CLI artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.cli_artifact }}
          path: staging/${{ matrix.cli_artifact }}
          if-no-files-found: error

      - name: Upload LSP artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.lsp_artifact }}
          path: staging/${{ matrix.lsp_artifact }}
          if-no-files-found: error

  upload-assets:
    name: Upload Release Assets
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v7
        with:
          path: artifacts

      - name: Flatten artifacts
        run: |
          mkdir -p release
          find artifacts -type f -exec mv {} release/ \;
          ls -la release/

      - name: Generate checksums
        run: |
          cd release
          sha256sum * > checksums-sha256.txt
          cat checksums-sha256.txt

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}